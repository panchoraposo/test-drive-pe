apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: neuralbank-frontend
  title: "Neuralbank: Frontend"
  description: "Creates Neuralbank's frontend for credit visualization and management. Includes Tekton CI/CD pipeline, ArgoCD auto-deployment via ApplicationSet, and DevSpaces support. Webhooks are created automatically by a background CronJob."
  tags:
    - frontend
    - html
    - neuralbank
    - recommended
spec:
  owner: platform-engineering
  type: website
  parameters:
    - title: Application Information
      required:
        - name
      properties:
        name:
          title: Application Name
          type: string
          default: neuralbank-frontend
          pattern: "^[a-z0-9-]+$"
          ui:autofocus: true
        description:
          title: Description
          type: string
          default: "Neuralbank credit visualization frontend"
        backendName:
          title: Backend component name
          type: string
          default: neuralbank-backend
          description: "Name used when you created the backend template (for example: neuralbank-backend)."
        clusterDomain:
          title: Cluster Domain (apps base domain)
          type: string
          ui:readonly: true
          description: Auto-populated from the environment installation.
          pattern: "^[a-z0-9.-]+$"
  steps:
    - id: fetch-skeleton
      name: Generate Skeleton
      action: fetch:template
      input:
        url: ./skeleton
        values:
          name: ${{ parameters.name }}
          uniqueName: "${{ user.entity.metadata.name }}-${{ parameters.name }}"
          owner: ${{ user.entity.metadata.name }}
          description: ${{ parameters.description }}
          namespace: "${{ user.entity.metadata.name }}-neuralbank"
          imageRegistry: "image-registry.openshift-image-registry.svc:5000"
          clusterDomain: ${{ parameters.clusterDomain }}
          backendName: ${{ parameters.backendName }}
          backendUniqueName: "${{ user.entity.metadata.name }}-${{ parameters.backendName }}"
          backendApiName: "${{ user.entity.metadata.name }}-${{ parameters.backendName }}-api"
    - id: publish
      name: Publish to Gitea
      action: publish:gitea
      input:
        repoUrl: "gitea-gitea?owner=ws-${{ user.entity.metadata.name }}&repo=${{ parameters.name }}"
        description: ${{ parameters.description }}
        defaultBranch: main
        repoVisibility: public
    - id: register
      name: Register in Catalog
      action: catalog:register
      input:
        repoContentsUrl: "https://gitea-gitea.${{ parameters.clusterDomain }}/ws-${{ user.entity.metadata.name }}/${{ parameters.name }}/src/branch/main/"
        catalogInfoPath: catalog-info.yaml
    - id: notify
      name: Send Email Notification
      action: http:backstage:request
      input:
        method: POST
        path: /proxy/mailpit/api/v1/send
        headers:
          Content-Type: application/json
        body:
          From:
            Email: "developer-hub@redhat.local"
            Name: "Red Hat Developer Hub"
          To:
            - Email: "${{ user.entity.metadata.name }}@redhat.local"
              Name: "${{ user.entity.metadata.name }}"
          Subject: "Neuralbank Platform — Frontend Application Deployed"
          Text: "Neuralbank Platform Notification — Your frontend application ${{ user.entity.metadata.name }}-${{ parameters.name }} has been provisioned in namespace ${{ user.entity.metadata.name }}-neuralbank. The CI/CD pipeline is now building and deploying your application. Repository: ws-${{ user.entity.metadata.name }}/${{ parameters.name }}"
          HTML: "<div style='font-family:Red Hat Text,Helvetica,Arial,sans-serif;max-width:600px;margin:0 auto;border:1px solid #e0e0e0;border-radius:8px;overflow:hidden'><div style='background:#EE0000;padding:24px 32px;text-align:center'><h1 style='color:#fff;margin:0;font-size:22px;font-weight:600;letter-spacing:0.5px'>Neuralbank Platform</h1><p style='color:#ffcccc;margin:4px 0 0;font-size:12px'>Red Hat Developer Hub</p></div><div style='padding:32px'><h2 style='color:#151515;margin:0 0 16px;font-size:18px'>Frontend Application Deployed</h2><p style='color:#6a6e73;font-size:14px;line-height:1.6;margin:0 0 20px'>Your application has been successfully provisioned on the Neuralbank platform.</p><table style='width:100%;border-collapse:collapse;font-size:14px;margin-bottom:24px'><tr style='border-bottom:1px solid #f0f0f0'><td style='padding:10px 0;color:#6a6e73;width:140px'>Component</td><td style='padding:10px 0;color:#151515;font-weight:500'>${{ user.entity.metadata.name }}-${{ parameters.name }}</td></tr><tr style='border-bottom:1px solid #f0f0f0'><td style='padding:10px 0;color:#6a6e73'>Namespace</td><td style='padding:10px 0;color:#151515;font-weight:500'>${{ user.entity.metadata.name }}-neuralbank</td></tr><tr style='border-bottom:1px solid #f0f0f0'><td style='padding:10px 0;color:#6a6e73'>Repository</td><td style='padding:10px 0;color:#151515;font-weight:500'>ws-${{ user.entity.metadata.name }}/${{ parameters.name }}</td></tr><tr><td style='padding:10px 0;color:#6a6e73'>Pipeline</td><td style='padding:10px 0;color:#151515;font-weight:500'><span style='background:#faeae8;color:#c9190b;padding:2px 8px;border-radius:12px;font-size:12px'>Building</span></td></tr></table><p style='color:#6a6e73;font-size:13px;line-height:1.5;margin:0 0 24px'>The CI/CD pipeline (Tekton) is building the container image and deploying to your namespace. You can monitor progress from the <strong>CI</strong> tab in Developer Hub.</p></div><div style='background:#f0f0f0;padding:16px 32px;text-align:center;font-size:11px;color:#6a6e73'>Neuralbank Platform &bull; Powered by Red Hat Developer Hub</div></div>"
  output:
    links:
      - title: Repository
        url: ${{ steps['publish'].output.remoteUrl }}
      - title: View in Catalog
        icon: catalog
        entityRef: ${{ steps['register'].output.entityRef }}
