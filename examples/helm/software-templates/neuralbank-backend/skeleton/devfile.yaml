schemaVersion: 2.2.2
metadata:
  name: ${{values.name}}
  version: 1.0.0
  displayName: "${{values.name}} - Neuralbank Backend"
  description: ${{values.description}}
  language: Java
  projectType: Quarkus
projects:
  - name: ${{values.name}}
    git:
      remotes:
        origin: https://gitea-gitea.${{values.clusterDomain}}/ws-${{values.owner}}/${{values.name}}.git
      checkoutFrom:
        revision: main
components:
  - name: tools
    container:
      image: registry.redhat.io/devspaces/udi-rhel9:latest
      env:
        # Ensure Code - OSS opens with a folder (avoids \"No folder opened\" after cloning).
        - name: VSCODE_DEFAULT_WORKSPACE
          value: /projects
      memoryRequest: 1Gi
      memoryLimit: 4Gi
      cpuRequest: "500m"
      cpuLimit: "2"
      mountSources: true
      volumeMounts:
        - name: m2
          path: /home/user/.m2
  - name: m2
    volume:
      size: 2Gi
commands:
  - id: 01-build
    exec:
      component: tools
      commandLine: |
        set -eu
        # In DevSpaces we only need to compile/download deps; `package` runs Quarkus augmentation,
        # which has proven flaky on some workspace filesystems.
        mvn -ntp -DskipTests compile
      workingDir: ${PROJECT_SOURCE}
      group:
        kind: build
        isDefault: true
  - id: 02-run
    exec:
      component: tools
      commandLine: |
        set -eu
        mvn -ntp quarkus:dev -Dquarkus.http.host=0.0.0.0 -Dquarkus.http.proxy.proxy-address-forwarding=true
      workingDir: ${PROJECT_SOURCE}
      group:
        kind: run
        isDefault: true

